# Configuración de Email para Producción
# Copia este archivo como .env y configura tus credenciales reales

# === SENDGRID CONFIGURATION (Recomendado) ===
# Obtén tu API Key en: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.tu_api_key_aqui

# Email desde el cual se enviarán los mensajes
# Debe ser un dominio verificado en SendGrid
FROM_EMAIL=noreply@tudominio.com
FROM_NAME=Tu App Name

# === SMTP CONFIGURATION (Alternativa) ===
# Para usar Gmail, Outlook, etc.
SMTP_EMAIL=tu_email@gmail.com
SMTP_PASSWORD=tu_password_de_aplicacion

# === AWS SES CONFIGURATION (Para usuarios de AWS) ===
AWS_ACCESS_KEY_ID=tu_access_key
AWS_SECRET_ACCESS_KEY=tu_secret_key
AWS_REGION=us-east-1

# === CONFIGURACIÓN GENERAL ===
# Entorno: development | production
NODE_ENV=production

# URL base de tu aplicación (para links en emails)
APP_URL=https://tuapp.com